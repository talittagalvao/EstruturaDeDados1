#include <stdlib.h>
#include "list.h"

int main(){
    /*Tlist *list = create();
    int value;

    for(int i = 1; i <= 3; i++){
    printf("Insira um elemento: ");
    scanf("%d", &value);
    list = ordered_insert(list,value);
    print_list(list);
    printf("\n-------------\n\n");
    }

    Tlist *s = search (list, 5);

    if(s){
        printf("\n>>Elemento %d encontrado na lista<<\n\n", s->info);
    }else{
        printf("\n>>Elemento nao encontrado<<\n\n");
    }

    printf("\n-------------\n\n");
    list = remove_elem (list, 5);
    print_list(list);

    
    clear(list);*/

    random_draw();

    return 0;


}